spring:
  application:
    name: tp-bd2
  output:
    ansi:
      enabled: ALWAYS
  
  mongodb:
    host: localhost
    port: 27017
    database: tp_bd2
    authentication-database: admin
    username: admin
    password: admin123

app:
  json-output: /app/data/ventas.json

server:
  port: 8080
